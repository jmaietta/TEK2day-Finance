# PINNED EXACTLY. Every version here is what the containers ALREADY resolved
# to on 19 Aug 2026 - read from the GitHub Actions build log, not chosen. This
# changes nothing about what runs today; it stops it changing tomorrow.
#
# WHY: this file used to say "yfinance>=0.2.31" and never mention pandas or
# numpy at all, so every image rebuild silently re-resolved all 54 packages to
# whatever was newest that day. The whole platform sits on yfinance. On 13 Aug
# 2026 a rebuild left the daily price pull failing on every one of 9,911
# tickers, and the job reported SUCCESS for a week while Firestore went stale.
#
# The transitive packages are pinned too, deliberately. Pinning only the direct
# ones leaves pandas and numpy - the libraries the failure actually surfaced in
# - free to move underneath a "pinned" build.
#
# TO UPGRADE: change a version here on purpose, in its own commit, and watch
# one nightly run before trusting it.
#
# -- direct dependencies -----------------------------------------------------
yfinance==1.6.0
google-cloud-firestore==2.28.1
click==8.4.2
python-dotenv==1.2.3
fastapi==0.141.1
uvicorn==0.52.4
jinja2==3.1.6
rich==15.0.0
requests==2.34.2
plotext==5.3.2
xlsxwriter==3.2.9
# Read by architecture_check.py, which verifies architecture.yaml still
# describes reality. Added deliberately on 20 Aug 2026, not pulled in as a
# transitive dependency.
PyYAML==6.0.3

# -- transitive, pinned so a rebuild cannot move them ------------------------
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
beautifulsoup4==4.15.0
certifi==2026.7.22
cffi==2.1.1
charset_normalizer==3.5.1
cryptography==50.0.0
curl_cffi==0.16.0
google-api-core==2.34.0
google-auth==2.56.3
google-cloud-core==2.6.1
googleapis-common-protos==1.75.1
grpcio==1.83.0
grpcio-status==1.83.0
h11==0.16.0
idna==3.19
lxml==6.1.2
markdown-it-py==4.2.0
MarkupSafe==3.0.3
mdurl==0.1.2
multitasking==0.0.13
numpy==2.5.2
pandas==3.0.5
peewee==4.3.0
platformdirs==4.11.3
proto-plus==1.28.3
protobuf==7.35.1
pyasn1==0.6.4
pyasn1-modules==0.4.2
pycparser==3.0
pydantic==2.13.4
pydantic-core==2.46.4
pygments==2.21.0
python-dateutil==2.9.0.post0
pytz==2026.3.post1
six==1.17.0
soupsieve==2.9.2
starlette==1.6.0
typing-extensions==4.16.0
typing-inspection==0.4.4
urllib3==2.7.0
websockets==17.0.1
